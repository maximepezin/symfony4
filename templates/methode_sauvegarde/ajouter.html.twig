{# templates/methode_sauvegarde/ajouter.html.twig #}
{% set title = 'Ajouter une méthode de sauvegarde' %}

{% extends 'layout.html.twig' %}

{% block title %}{{ title }} &middot; {{ parent() }}{% endblock %}

{% block body %}
                    <div class="mb20">
                        <a class="btn btn-default" href="{{ path('base_materiel_methodes_sauvegarde') }}">
                            <i class="fa fa-caret-left"></i>
                            Retour
                        </a>
                    </div>
                    <div class="panel mb0">
                        <div class="panel-heading border">
                            {{ title }}
                        </div>
                        <div class="panel-body">
                            <div class="row no-margin">
                                <div class="col-lg-12">
{% include 'methode_sauvegarde/form.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
{% endblock %}
