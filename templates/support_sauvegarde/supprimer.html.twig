{# templates/support_sauvegarde/supprimer.html.twig #}
{% set title = 'Supprimer un support de sauvegarde' %}

{% extends 'layout.html.twig' %}

{% block title %}{{ title }} &middot; {{ parent() }}{% endblock %}

{% block body %}
                    <div class="panel mb0">
                        <div class="panel-heading border">
                            {{ title }}
                        </div>
                        <div class="panel-body">
                            <p>
                                Etes-vous certain de vouloir supprimer le support de sauvegarde <strong>{{ support_sauvegarde.libelle }}</strong> ?
                            </p>
                            <form action="{{ path('base_materiel_support_sauvegarde_supprimer', { 'id': support_sauvegarde.id }) }}" method="post">
                                <a href="{{ path('base_materiel_supports_sauvegarde') }}" class="btn btn-default">
                                    <i class="fa fa-caret-left"></i>
                                    Retour
                                </a>
                                <input type="submit" value="Supprimer" class="btn btn-danger" />
                                {{ form_rest(form) }}
                            </form>
                        </div>
                    </div>
{% endblock %}
